<Page
    x:Class="TestHost.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestHost"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    RequestedTheme="Light"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="0" x:Name="input" FontFamily="Consolas" TextWrapping="NoWrap" AcceptsReturn="true" Text="1+1" />
        <Button Grid.Column="1" VerticalAlignment="Top" x:Name="eval">Eval</Button>
        <TextBox Grid.Column="2" x:Name="output" TextWrapping="NoWrap" AcceptsReturn="true" />
    </Grid>
</Page>
